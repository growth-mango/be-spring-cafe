{{> header }}

<div class="container" id="main">
    <div class="col-md-10 col-md-offset-1">
        <div class="panel panel-default">
            <table class="table table-hover">
                <thead>
                <tr>
                    <th>#</th> <th>글쓴이</th> <th>제목</th> <th>내용</th><th></th>
                </tr>
                </thead>

                <tbody>
                {{#articles}}

                    <tr>
                        <th scope="row">{{id}}</th> <td>{{writer}}</td> <td>{{title}}</td> <td>
                        {{content}}</td><td><a href="#" class="btn btn-success" role="button">수정</a></td>
                    </tr>

                {{/articles}}
                </tbody>
            </table>
        </div>
    </div>
</div>

{{> footer }}